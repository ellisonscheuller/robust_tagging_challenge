# ctl2_model

TriggerFlow ML pipeline for hardware-efficient trigger-level anomaly detection at the LHC.

## Branches

| Branch | Purpose |
|---|---|
| `docker_build` | Docker image builds (Kaniko) |
| `master` | Main ML pipeline (Kedro: load, preprocess, train, validate, compile) |

## Documentation

See [ctl2_model/README.md](ctl2_model/README.md) for full project documentation.
